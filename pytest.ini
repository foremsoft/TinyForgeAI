[pytest]
# Test discovery
testpaths = tests
python_files = test_*.py
python_functions = test_*

# Output options
addopts = -q --disable-warnings -ra

# Markers (for custom test markers)
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: marks tests as integration tests
