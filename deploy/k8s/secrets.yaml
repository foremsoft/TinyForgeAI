apiVersion: v1
kind: Secret
metadata:
  name: tinyforge-secrets
  namespace: tinyforge
  labels:
    app: tinyforge
type: Opaque
stringData:
  # Dashboard API authentication
  TINYFORGE_AUTH_USERNAME: "admin"
  TINYFORGE_AUTH_PASSWORD: "changeme123"  # CHANGE THIS IN PRODUCTION!
  TINYFORGE_API_KEY: "tinyforge-dev-key-change-in-production"

  # HuggingFace token (optional, for private models)
  # HF_TOKEN: "your-huggingface-token"
---
# Optional: TLS certificate for ingress
# apiVersion: v1
# kind: Secret
# metadata:
#   name: tinyforge-tls
#   namespace: tinyforge
# type: kubernetes.io/tls
# data:
#   tls.crt: <base64-encoded-cert>
#   tls.key: <base64-encoded-key>
